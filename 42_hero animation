Hero Animation म्हणजे काय?

जेव्हा दोन वेगवेगळ्या स्क्रीन्सवर एकच widget असेल आणि दोन्ही widget ला एकच tag दिलेले असेल, तेव्हा Flutter त्या दोन widgets दरम्यान smooth animation करतो.
उदा: इमेजवर टॅप केल्यावर ती मोठी होऊन दुसऱ्या स्क्रीनवर पूर्ण दिसणे.

ex:
Hero(
  tag: "box",
  child: Container(
    width: 100,
    height: 100,
    color: Colors.blue,
  ),
);


first page :
import 'package:flutter/material.dart';

class FirstScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Hero Animation Example")),
      body: Center(
        child: GestureDetector(
          onTap: () {
            Navigator.push(
              context,
              MaterialPageRoute(builder: (_) => SecondScreen()),
            );
          },
          child: Hero(
            tag: "myHero",
            child: Image.network(
              "https://picsum.photos/200",
              width: 150,
            ),
          ),
        ),
      ),
    );
  }
}

second page :
class SecondScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Detail Screen")),
      body: Center(
        child: Hero(
          tag: "myHero",
          child: Image.network(
            "https://picsum.photos/200",
            width: 300,
          ),
        ),
      ),
    );
  }
}

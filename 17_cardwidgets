Card(
  child: Padding(
    padding: EdgeInsets.all(16),
    child: Text(
      "Basic Card",
      style: TextStyle(fontSize: 18),
    ),
  ),
);

| Property     | Use                      |
| ------------ | ------------------------ |
| elevation    | shadow                   |
| shape        | rounded corners, borders |
| color        | background color         |
| margin       | बाहेरील space            |
| child        | inside widget            |
| clipBehavior | image overflow control   |

complite profile

Card(
  elevation: 6,
  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),
  child: Container(
    padding: EdgeInsets.all(20),
    child: Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text("Flutter Card Design",
            style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),
        SizedBox(height: 10),
        Text(
          "This is a custom card with description text and spacing.",
          style: TextStyle(fontSize: 16),
        ),
        SizedBox(height: 10),
        Row(
          children: [
            Icon(Icons.favorite, color: Colors.red),
            SizedBox(width: 10),
            Text("Like"),
          ],
        )
      ],
    ),
  ),
);
